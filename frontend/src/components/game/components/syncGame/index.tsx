import "./styles.css";
import Loading from "../../../loading";

const SyncGame = () => (
  <div className="game-sync">
    <Loading />
    <span>Synchronizing Game...</span>
  </div>
);

export default SyncGame;
